@model Ramal.App.ViewModels.SetorViewModel

@{
    ViewData["Title"] = "Detalhes - " + Model.Nome;
}

<h1 class="mt-5 mb-4">@ViewData["Title"]</h1>


<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-warning text-white" supress-by-claim-name="Setor" supress-by-claim-value="Editar" asp-action="Edit" asp-route-id="@Model?.Id">Editar</a>
    <a class="btn btn-info" asp-action="Index">Voltar</a>
</div>
